To Run the program, Follow these steps:


1.  On console, type “python Apriori.py”
2. Console will prompt to enter the filename, you need to give the name of data file which should be in sparse matrix format.


      3.) After Enter “F1” or “FK_1” to run the program.